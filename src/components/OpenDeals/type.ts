export interface ICity {
    img: string;
    title: string;
    price: string;
    ticket: string;
    yield: string;
    day: string;
    sold: string;
}

export interface IResponse {
    error: {message: string}
}